PRIVATE HANDBOOK · DO NOT SHARE PUBLICLY · NMG.LABS
Forge 2 · Edition 1 — The Main
Hackathon
This is show time. One builder, one day, two AI agents. You will run Hermes (the
orchestrator / product owner) and OpenClaw (the coder) over Slack — agile,
with bots — and ship a real multi-tenant SaaS in a single focused day. You don't
write the features by hand. You direct the agents that do.
In-person · solo 10:30 AM →  6:00 PM IST $50 EastRouter credits
Stack: Laravel + MySQL + React Agile · Hermes × OpenClaw
BUILD WINDOW · 10:30 AM – 6:00 PM IST
07
TIME HRS
19
MIN
59
SEC
Keep an eye on the clock — but submissions stay open even after 6:00 PM. Late = reviewed
at the organizers' discretion. Quality & process over a frantic finish.
♪  PRESS PLAY · BUILD MODE
The Forge 2 · Edition 1 playlist
Queue it up, get your agents talking, and lock in for the day.
Preview


·
·
21 Hollow (feat. Bonn) MORTEN, ARTBAT, Bonn
22 Love Parade Cassian, YOTTO, Da Hool
NMG LABS FORGE 2· Mohit
CONTENTS
Everything you need — read it all once before you
start.
00 · The mission & the method (agile with bots)
01 · Format, timeline & what you submit
02 · EastRouter — your $50 of models
03 · The build — PulseDesk (problem statement)
04 · The required stack
05 · The agile AI-agent workflow
06 · Exact Git repo structure
07 · Evidence you must keep
08 · How you'll be judged (full rubric)
09 · Rules & restrictions
10 · FAQ — every doubt, pre-answered
11 · Submit your build
00 · THE MISSION & THE METHOD
Don't write the app. Orchestrate the agents that
write it.
In the qualifier you proved you could set up Hermes + OpenClaw and ship a tiny build.
Today is the real thing: you run a proper agile sprint cycle with AI agents and ship a non-
trivial, multi-tenant SaaS in a day.
The method we want you to practise is exactly how modern AI-native teams ship — “agile,
with bots.” Here's the loop in one breath:
spec   →   tight sprint (a few scoped issues)   →   Hermes plans & assigns in Slack
  →   OpenClaw codes + tests + reports   →   CI runs   →   you (human) review & merge
the PR   →   next sprint.
The principles we're grading you on (straight from how a real AI-native shop runs this):
Keep sprints tight and focused. Small, well-scoped sprints beat one giant “build
everything” prompt every time. A bloated sprint produces slop.
Two roles, in the open. Hermes is the product owner / orchestrator. OpenClaw is the
coder. They never talk privately — every handoff happens in Slack so a human (you)
stays in the loop.
Right model for the right job. Use a stronger reasoning model for planning/architecture
and a fast coding model for implementation (see EastRouter §02). Cluster your tasks by
difficulty.
Humans merge to main. Agents open PRs; you review and merge release candidates.
No bot auto-merges to main — bots are great at scope creep, so you gate it.
Stay consistent. Once an agent config works, don't keep “upgrading” it mid-event — it
creates chaos. Lock it, branch a new config if you must experiment.
What we are really testing: not “can you write a SaaS” — but can you make two
agents collaborate, under your direction, to produce working software with a clean
process and a clear audit trail. A smaller app with a flawless, well-evidenced agent
loop beats a bigger app with no visible orchestration.
01 · FORMAT & TIMELINE
One builder. One day. One shipped product.
THING DETAIL
Who Solo. Your own work, built live during the event. No teams, no pre-written feature code.
When 10:30 AM →  6:00 PM IST (one day). A live countdown is at the top of this page.
Start
clean
Create a fresh, empty repo at the start (an empty Laravel + React scaffold is fine —
features start at zero). Don't bring a pre-built app.
Models $50 of EastRouter credits (see §02). All agent model calls go through EastRouter.
Build The PulseDesk multi-tenant SaaS (§03), in the required stack (§04), via the agile AI-
agent workflow (§05).
Submit One submission: GitHub repo + (optional) live URL + evidence. Form at the bottom (§11).
Submissions don't auto-close at 6 PM — but the clock is real; finish your loop and
submit.
Pace yourself like a sprinter, not a marathoner. Suggested rhythm: 30 min spec &
agent setup →  3–4 tight sprints of ~75–90 min each →  leave the last 45 min for a final
CI pass, the README, your evidence, and submission. Don't start a brand-new sprint
after ~5:00 PM — land what you have clean.
02 · EASTROUTER — YOUR $50 OF MODELS
One endpoint. Frontier models. $50 on us.
EastRouter is an OpenAI- & OpenRouter-compatible gateway to East-Asian frontier models
— GLM 5.1, Kimi K2.6, MiniMax M2.7, DeepSeek V4 and more. Both OpenClaw and Hermes
work with it natively. You get $50 of credit for today.
Step 1 — Get your account & claim your $50
1. Go to dashboard.eastrouter.com and sign up with Google or GitHub. (A $10 trial
activates immediately — no card.)
2. Share the email / handle you registered with to your Forge captain (in person or in the
Forge Slack). They'll top your account up to $50.
3. In the dashboard, create an API key — it looks like sk-er_<id>_<secret>. Treat it like a
password.
4. Check your balance anytime: GET https://api.eastrouter.com/v1/balance with your key.
Step 2 — Point your agents at EastRouter
CLIENT BASE URL
OpenAI SDK / Hermes / OpenAI-mode tools https://api.eastrouter.com/v1
Claude-compatible (OpenClaw Anthropic mode /
Claude Code)
https://api.eastrouter.com/api/anth
ropic
OpenClaw (the coder) — Anthropic-compatible mode (recommended)
# in OpenClaw's environment / config
export ANTHROPIC_BASE_URL="https://api.eastrouter.com/api/anthropic"
export ANTHROPIC_AUTH_TOKEN="sk-er_//.your_key//."
# then run OpenClaw and select a model id, e.g. z-ai/glm-5.1
OpenClaw — OpenAI-compatible mode (alternative)
base_url = "https://api.eastrouter.com/v1"
api_key  = "sk-er_//.your_key//."
model    = "z-ai/glm-5.1"
Hermes (the orchestrator) — add EastRouter as an OpenAI-compatible provider
provider:  eastrouter
base_url:  https://api.eastrouter.com/v1
api_key:   sk-er_//.your_key//.
model:     deepseek/deepseek-v4-pro   # strong reasoning for planning/PO
# (drop to z-ai/glm-5.1 for faster planning if you prefer)
Step 3 — Pick the right model for the job (model segmentation)
MODEL ID BEST FOR CONTEXT
z-ai/glm-5.1 Recommended default. Strong all-round
coding + planning
200K
deepseek/deepseek-v4-pro Heavy reasoning — architecture, sprint
planning, cross-dependency calls (Hermes)
1M
moonshotai/kimi-k2.6 Long-horizon coding tasks (OpenClaw) 262K
minimax/minimax-m2.7 Agentic / tool-heavy workflows 197K
z-ai/glm-4.6 · z-ai/glm-4.5-
air · deepseek/deepseek-v4-
flash
Cheap, fast iterations — use these to save
credit on routine edits
128K–
1M
Budget tip: $50 is plenty for a full day if you keep sprints tight. Plan/architect with a
strong model (DeepSeek V4 / GLM 5.1), do the bulk of coding on GLM 5.1, and drop to
GLM 4.5 Air / DeepSeek V4 Flash for small repetitive edits. Watch /v1/balance.
Streaming + tool-calling are supported on GLM, Kimi and MiniMax.
All model usage must go through EastRouter for this event (that's what the credits are
for, and it keeps judging fair). Don't mix in other paid APIs. Local Ollama is allowed only
as a fallback if EastRouter is unreachable — note it in your README if you use it.
03 · THE BUILD
PulseDesk — a multi-tenant support-desk SaaS.
Build PulseDesk: a hosted help-desk / support-ticketing product that multiple companies
(tenants) use to manage customer support. Think a focused mini-Zendesk. It's deliberately
chosen because it has real depth (multi-tenancy, roles, SLAs, metrics) and decomposes
cleanly into tight sprints.
Features are tiered. 
 MUST  = the core you should land. 
 SHOULD  = depth that lifts you.
STRETCH  = wow. You are not expected to finish everything — get the Must tier solid, then
go as deep as your sprints allow.
MUST  Core — land these first
Multi-tenancy. Organizations (tenants). Every record belongs to an org; a user from Org
A can never see Org B's data. (Scope every query by organization_id.)
Auth & roles (Laravel Sanctum). Register / login. Roles: admin, agent, customer.
Tickets CRUD. Fields: subject, description, status (open / pending / resolved / closed),
priority (low / medium / high / urgent), requester (customer), assignee (agent),
tags/labels, created/updated timestamps.
Ticket conversation. Threaded replies on a ticket — a public reply (visible to the
customer) and an internal note (agents only).
List + filters + search. A ticket list/board filterable by status, priority and assignee, with
text search on subject/body.
REST API + React frontend. A documented JSON API consumed by your React app (no
server-rendered-only shortcuts for the core flows).
Seeded demo data. A seeder that creates 1 org, an admin, 2 agents, 2 customers and
~12 tickets so a judge sees a populated app instantly.
SHOULD  Depth — where good builds pull ahead
SLA policies & timers. Per-priority response & resolution targets; a live “time remaining /
breached” indicator on each ticket.
Queues & assignment. “Unassigned”, “My tickets”, reassignment; an agent can claim a
ticket.
Activity log / audit trail per ticket (status change, assignment, replies) with who + when.
Dashboard metrics. Open by status/priority, avg first-response time, SLA-breach rate,
tickets created per day.
Notifications. An in-app notification centre (or a logged/webhook notification) when a
ticket is assigned or replied to.
STRETCH  Wow — only if your sprints are flying
Canned responses / macros · ticket merge · CSAT rating after resolution.
A separate customer portal view · bulk actions · a public, rate-limited “submit a ticket”
API.
Real-time updates (polling or websockets) · full-text search · CSV export.
This is the brief — build PulseDesk, not something else. Building a different app is the
single most common way to get disqualified. If a feature is ambiguous, make a sensible
call, write it in your README, and move on.
04 · THE REQUIRED STACK
Same family you trained on — so judging is fair and
fast.
LAYER REQUIRED
Language PHP 8.2+
Backend Laravel 11 (REST API). Auth via Laravel Sanctum.
Database MySQL 8 (use migrations + seeders; no SQLite for the final build).
Frontend React 19 + Vite, styled with Tailwind. (Blade is fine for auth scaffolding, but the app UI
is React.)
Tests Pest or PHPUnit feature tests on the API (at least the core ticket + tenancy flows).
CI A GitHub Actions workflow that installs, migrates and runs tests on each PR.
Libraries, scaffolding and component kits are allowed and encouraged — Laravel and React
are themselves scaffolds. What must be agent-built is the PulseDesk features (models, API,
UI, logic), not boilerplate.
Why a fixed stack? So every build clones, migrates and runs the same way, and
judges compare like-for-like. It also matches what you practised in the qualifier, so no
surprises today.
05 · THE AGILE AI-AGENT WORKFLOW
This is the heart of the event. Get this right.
Your agents — 2 required, more earn bonus
The core is the Hermes + OpenClaw pair. You are not limited to two — adding a reviewer or
a CI/deploy agent is how real AI-native teams actually run, and it earns bonus marks (§08).
But quality beats quantity: a tight 2-agent loop scores higher than a sloppy 4-agent one.
AGENT REQUIRED? ROLE
Hermes Required Orchestrator / Product Owner — turns the spec into a sprint
backlog & scoped issues, assigns work, reviews reports, keeps
memory, runs a cron heartbeat.
OpenClaw
(coder)
Required The primary coder & workhorse of the event — implements each
assigned issue, runs code + tests, opens a PR, reports in Slack
(What I Did / What's Left / What Needs Your Call). Its config is
weighted heavily — get it right.
OpenClaw #2
— reviewer /
QA
Bonus A second OpenClaw that reviews the coder's PRs, runs the tests,
and posts problems back before you merge — the “error →
decompose →  fix” loop.
CI / deploy
agent
Bonus A Hermes or OpenClaw dedicated to the stack: runs CI, reports to
#ci-cd, handles deploy. (Eean's “stack agent.”)
Your OpenClaw config must be tight — it's weighted heavily
OpenClaw is the workhorse, and a half-configured coder is the single most common failure.
Your committed agents/openclaw/openclaw.json must contain (secrets redacted to
${ENV}):
MUST INCLUDE WHY WE CHECK IT
Model — EastRouter base_url (…/v1 or
…/api/anthropic), api_key: ${EASTROUTER_API_KEY},
real coding model (e.g. z-ai/glm-5.1)
Proves the coder runs on
EastRouter credit + a valid model
— not a stub.
Slack — enabled; app + bot tokens via env; channels #agent-
coder, #agent-log, #ci-cd
Proves it's wired into the loop, not
run silently in a terminal.
Workspace — working dir + command execution enabled Proves it can actually write & run
code/tests (not just chat).
Skills / tools — any tools or custom skill you gave it Shows real capability config, not
defaults.
Prove the model is actually used. Known OpenClaw gotcha (issue #7211): a model can
be configured but silently not used. Commit a log or screenshot showing your
EastRouter model id being hit while OpenClaw answers — a config file alone is not
proof, and we score the proof.
Your Hermes config
Your committed agents/hermes/hermes-config.yaml must show: provider eastrouter +
base URL + a planning model (e.g. deepseek/deepseek-v4-pro or z-ai/glm-5.1), memory
enabled, and its Slack channels (#sprint-main, #agent-coder, #human-review).
Required Slack channels (everything in the open)
CHANNEL PURPOSE
#sprint-main You ↔  Hermes. Spec, sprint planning, decisions, status.
#agent-coder Hermes ↔  OpenClaw. Task handoffs & clarifications.
#agent-log OpenClaw's structured reports (What I Did / Left / Needs Your Call).
#ci-cd Build / test / CI results (paste or pipe them here).
#human-review Release-candidate approvals — where you sign off before merge.
The sprint loop you must run ( ≥  2 sprints)
1. Spec →  backlog. Tell Hermes the goal; have it produce a sprint backlog of small issues
before any code is written. Save it to sprints/sprint-0N.md.
2. Assign. Hermes hands a scoped issue to OpenClaw in #agent-coder.
3. Build + report. OpenClaw implements, runs tests, opens a PR, and posts its report to
#agent-log.
4. CI. GitHub Actions runs on the PR; results land in #ci-cd.
5. Human review & merge. You review the PR in #human-review and merge it yourself. No
bot merges to main.
6. Repeat with the next tight sprint. Aim for 3–4 small sprints rather than 1 big one.
Minimum bar: at least 2 real sprints, each with a saved sprint doc, a genuine
Hermes → OpenClaw handoff in Slack, a PR you merged, and OpenClaw reports in
#agent-log. More tight sprints = a stronger process score.
Human-in-the-loop is mandatory. Agents must communicate through Slack channels,
not silently. You merge to main. If you let a bot run unsupervised straight to main,
you lose the entire process score.
06 · EXACT GIT REPO STRUCTURE
Put it exactly here — so judging is fast and fair.
Name your repo forge2/<yourname>, make it public, and match this layout exactly.
Everything — code, configs, logs, Slack proof, screenshots — is committed here. No
external links. Anything a judge can't find in the repo, they can't credit.
⬇  Don't build the folders by hand — download the starter skeleton. It's the exact
structure below with ready-to-fill templates (README, ARCHITECTURE, SUBMISSION
checklist, agent-log, sprint docs, redacted agent configs, CI workflow, .env.example s,
evidence folders). Structure only — zero app features.
Download the PulseDesk starter kit (.zip) →
Unzip →  rename to forge2/<yourname> →  git init →  then point your agents at it and
start sprint 1.
forge2/<yourname>/
├─  README.md                 # what it is · stack · EXACT run steps · live URL (if any) · m
├─  ARCHITECTURE.md           # data model · API routes · multi-tenancy approach · key decis
├─  SUBMISSION.md             # the submission checklist, ticked, with in-repo paths
├─  agent-log.md              # the human → Hermes → OpenClaw loop, in order (real, not template
├─  backend/                            # Laravel 11 + MySQL
│   ├─  app/Models/                      # Organization, User, Ticket, Comment, SlaPolicy, Ac
│   ├─  app/Http/Controllers/Api/
│   ├─  app/Policies/                    # tenant + role authorization
│   ├─  database/migrations/
│   ├─  database/seeders/                # 1 org, admin, 2 agents, 2 customers, ~12 tickets
 
│   ├─  routes/api.php
│   ├─  tests/                           # Pest/PHPUnit feature tests (tickets + tenancy isol
│   └─  .env.example                     # MySQL + EASTROUTER_API_KEY placeholders (NO real s
├─  frontend/                           # React 19 + Vite + Tailwind
│   ├─  src/
│   └─  .env.example                     # VITE_API_URL
├─  agents/
│   ├─  hermes/hermes-config.yaml        # REAL Hermes config (provider=eastrouter, model) — 
│   ├─  openclaw/openclaw.json           # REAL OpenClaw config (base_url, model, slack) — se
│   └─  skills/                          # any custom skill
├─  sprints/
│   ├─  sprint-01.md                     # goal + issue list + outcome
│   ├─  sprint-02.md                     # ( ≥ 2 sprints — one file each)
│   └─  …
├─  slack-export/                       # REAL unzipped Slack export (per-channel JSON folde
│   └─  screenshots/                     # …OR per-channel screenshots if export isn't availa
│                                       #   sprint-main-01.png · agent-coder-01.png · agent-
│                                       #   · ci-cd-01.png · human-review-01.png
├─  evidence/
│   └─  screenshots/                     # app + agents-running + CI screenshots — NN-what-it
│                                       #   e.g. 01-ticket-list.png · 02-ticket-detail.png
│                                       #   03-dashboard.png · 04-openclaw-gateway.png · 05-
├─  .github/workflows/ci.yml           # CI: composer install · migrate · run tests
└─  (no video, no Google Drive — every artifact is committed in this repo)
Evidence map — exactly what goes where
ARTIFACT EXACT LOCATION IN REPO REQUIRED?
Hermes config (real, secrets
redacted)
agents/hermes/hermes-
config.yaml
Yes
OpenClaw config (real, secrets
redacted)
agents/openclaw/openclaw.js
on
Yes
The agent loop
(your → Hermes → OpenClaw)
agent-log.md Yes
Sprint docs ( ≥ 2) sprints/sprint-0N.md Yes
Slack proof slack-export/ (export) or
slack-export/screenshots/
(per channel)
Yes
App + agents-running + CI
screenshots
evidence/screenshots/ Yes
CI workflow .github/workflows/ci.yml +
green run on Actions tab
Yes
Demo video — None — no video this year
(live in-person demo
instead)
Never commit real secrets. Commit .env.example with placeholders, not your real
.env or your sk-er_ key. Redact tokens in your agent configs to ${ENV_VAR}.
Leaked keys = a deduction (and someone will spend your credit).
07 · EVIDENCE — ALL OF IT LIVES IN YOUR GIT REPO
Everything goes in the repo. No Drive, no video links,
no exceptions.
Read this carefully — it's new and it's strict. This year every artifact must be
committed to your Git repository: screenshots, Slack export, agent configs, logs,
sprint docs — all of it. We will not open Google Drive, YouTube, Loom, or any external
link for evidence. There is no video. If something isn't in the repo (or its screenshot
isn't committed), it does not count. This keeps judging fair, fast and permanent — and
it's the #1 way people lost earned points in the qualifier.
Capture as you go and commit to these exact locations:
1 · Agent configs →  agents/
agents/hermes/hermes-config.yaml (or .json) — your real Hermes config: provider
eastrouter, base URL, model, Slack settings. Redact secrets to ${EASTROUTER_API_KEY}
/ ${SLACK_BOT_TOKEN} — never the real values.
agents/openclaw/openclaw.json — your real OpenClaw config: EastRouter base URL,
model, Slack channels. Secrets redacted the same way.
agents/skills/ — any custom skill you gave an agent.
2 · The agent loop →  agent-log.md  (repo root) + sprints/
agent-log.md — the real loop in order: your prompt to Hermes →  Hermes'
plan/assignment →  OpenClaw's report (What I Did / What's Left / What Needs Your Call).
Real text, not a template.
sprints/sprint-01.md, sprint-02.md, … — one file per sprint: goal, issue list, what
shipped, what slipped.
3 · Slack proof →  slack-export/  (read this fully)
Your Slack is the audit trail of your agent loop — it's how we verify the work is real. Provide
it one of two ways (both is best), committed in the repo:
Option A — real Slack export (preferred)
1. In Slack: Workspace name ▸  Tools & settings ▸  Workspace settings ▸  Import/Export
Data ▸  Export.
2. Choose date range “Entire history” and Start export. (On the Free plan a workspace
owner can export all public channels — that's exactly our five, so make your channels
public.)
3. Download the .zip when ready, unzip it, and commit the unzipped contents into
slack-export/.
4. The export contains channels.json, users.json, and a folder per channel with dated
.json message files — exactly what we cross-check against your git & CI timeline.
Option B — per-channel screenshots (if export is locked on your plan)
Commit clear screenshots to slack-export/screenshots/, one set per channel, named by
channel: sprint-main-01.png, agent-coder-01.png, agent-log-01.png, ci-cd-01.png,
human-review-01.png. Each must show real agent messages with visible content +
timestamps — not just a “bot joined the channel” line.
What each channel must actually show
CHANNEL MUST CONTAIN
#sprint-main You giving Hermes a sprint goal; Hermes posting the plan/backlog back.
#agent-coder Hermes assigning a scoped issue to OpenClaw (the handoff).
#agent-log OpenClaw's real reports — What I Did / What's Left / What Needs Your Call.
#ci-cd Test/CI results for at least one PR.
#human-review You approving a release candidate before you merged it.
How judges use your Slack export: we build three independent timelines — your
Slack messages, your git/PR history, and your CI runs — and they must line up.
Genuine work agrees across all three; fabricated or pasted-after-the-fact logs don't,
and that's a disqualification. So just let it happen naturally and export it — don't
reconstruct it later.
4 · Screenshots →  evidence/screenshots/
Commit these as PNGs, named NN-what-it-is.png (e.g. 01-app-ticket-list.png):
App, working: ticket list/board · a ticket detail with its conversation · the
dashboard/metrics · the login screen.
Agents, running: the OpenClaw gateway in your terminal · Hermes running.
Multi-tenancy proof (optional but strong): the same view logged in as Org A vs Org B
showing different data.
CI: a green GitHub Actions run (also visible on your repo's Actions tab).
5 · History & CI →  in the repo itself
Incremental commits, PRs per issue/sprint, merges done by you (we read your git/PR
history on GitHub directly).
.github/workflows/ci.yml committed, with at least one green run on the Actions tab.
No video — and that's on purpose. Instead of a demo video, your README.md must
contain exact run steps, and because the event is in-person, a judge can ask you to
run PulseDesk live from a fresh clone. That live run + your committed screenshots
fully replace any video.
08 · HOW YOU'LL BE JUDGED
The full rubric — no surprises.
Every submission is cloned and inspected on real evidence. We weight the agent process
as heavily as the working app — this is an orchestration event. Score is out of 100.
CRITERION WEIGHT WHAT EARNS FULL MARKS
Agent orchestration loop
(Hermes ↔ OpenClaw ↔ you)
20 A genuine, repeated
human → Hermes → OpenClaw → report loop across the
sprints — the single biggest slice. Real handoffs, real
reports, clear role separation, all in Slack.
OpenClaw configuration &
coder execution
16 A tight, real openclaw.json (EastRouter model + Slack
+ workspace/command-exec + tools), proven to run on
your EastRouter model, that actually wrote & ran the
code. This is weighted heavily — OpenClaw is the
workhorse.
App works end-to-end 14 PulseDesk actually runs (fresh clone or live URL): Must
tier solid, Should/Stretch add depth. We click through
real flows.
Multi-tenant isolation
security-gated
12 Org A genuinely cannot read/modify Org B's data; tenant
derives from the auth session, not a client id. We run an
adversarial cross-tenant probe.
Hermes orchestration & PO
setup
10 Real hermes-config.yaml on a planning model with
memory; Hermes genuinely decomposed the spec,
planned sprints and assigned work.
Sprints →  CI/CD →  human-
merged PRs
10 ≥ 2 tight scoped sprints with docs; CI runs on PRs; you
are the merge actor, agents are the commit authors; no
bot auto-merge to main.
Slack workspace + export
quality
6 All 5 channels used with real agent traffic; a clean,
complete Slack export/screenshots committed that
cross-checks against git + CI.
Code & architecture 5 Proper Laravel 11 + MySQL + React; clean REST API;
sound data model; passing tests; ARCHITECTURE.md
present; no copy-paste sprawl.
Repo & docs 4 Matches the required layout; clear README run steps;
honest SUBMISSION.md; all evidence committed in-repo.
Live demo (in-person) 3 It runs on demand from a fresh clone (or your live URL)
when a judge asks — proving it's real and yours.
Bonus — up to +5 (capped at 100): genuine additional agents that really participated
in the loop — a second OpenClaw reviewer/QA agent (+ up to 3) and/or a CI/deploy
agent (+ up to 2). They must show real activity in Slack + the repo, not just exist.
Hard gates & caps
DQ: no public/clonable repo · built the wrong thing (not PulseDesk) · fabricated
evidence (your Slack / git / CI timelines contradict each other) · >75% plagiarised or a
forked existing app passed off as yours.
Capped low if: no genuine two-agent loop (it's just hand-coded) · a confirmed cross-
tenant data leak · the app is dead / localhost-only / frontend-with-no-backend.
Penalty for model calls not run through EastRouter (other paid keys), unless EastRouter
was down and you documented an Ollama fallback.
Not the required stack (no Laravel + MySQL backend / no React app UI).
How we verify (so you know it's fair)
Judges clone your repo and run migrate /-seed + your frontend; read your configs, agent-
log.md and sprint docs; open your Slack evidence; and cross-check three independent
timelines — your Slack export, your git/PR history, and your CI runs — which must agree.
We use server-side GitHub timestamps (commit dates are forgeable, so they don't count)
and confirm you are the PR merge actor. We run an adversarial multi-tenant probe (sign in
as Org A, try to read Org B). Because it's in-person, a judge may ask you to run it live from
a fresh clone. Borderline scores get a second independent review, and the top builds are
hand-validated before any winner is announced. You'll get a transparent written breakdown
on the results page, exactly like the qualifier.
The honest path always wins. A modest app with a real, well-documented agent loop
scores far better than an impressive-looking app with no visible orchestration or with
faked evidence. Build real, log everything.
09 · RULES & RESTRICTIONS
The short, firm list.
Solo. Your own work, built during the event window. Helping each other learn is great;
sharing code/repos is not.
Start fresh. New repo today. Empty framework scaffolds are fine; pre-built PulseDesk
features are not.
Required stack (§04) and required workflow (§05) — both are graded, both are
required.
EastRouter for all model calls (§02). Keep usage in your account; don't share keys.
Build PulseDesk (§03) — not a different app.
Humans merge to main. Agents open PRs; you approve.
Keep evidence (§07) and match the repo layout (§06).
No fabricated evidence. Honesty about what's done vs not done is rewarded; faking is
disqualifying.
Open-source libraries allowed. Credit anything substantial in your README.
Be excellent to each other and to the space. Code of conduct applies.
Allowed vs not allowed — the complete list
✓  Allowed
Framework scaffolds: laravel new,
Vite/React scaffold,
Breeze/Jetstream for auth,
Tailwind/shadcn/UI kits.
✕  Not allowed
Pre-built PulseDesk features,
bringing an existing app, or starting
before 10:30.
Teams or sharing code with another
participant — this is solo.
Any open-source library or package
(credit substantial ones in your
README).
Any editor/IDE (VS Code, JetBrains,
etc.) as your personal editor.
Switching EastRouter models freely
during the day; adding the bonus
reviewer/CI agents.
Reading docs, Stack Overflow, AI
chat to learn / debug.
Deploying to any host (bonus); a
clean local run is fully accepted too.
Resubmitting as many times as you
like (latest entry is scored).
Asking a Forge captain about
logistics (Wi-Fi, credits, timing).
Building anything other than
PulseDesk.
Model calls not through EastRouter
(other paid keys) — except a
documented Ollama fallback if
EastRouter is down.
Hand-coding the features yourself
with no real agent loop (the agents
build the features).
Bots auto-merging to main —
humans merge.
Fabricated/edited evidence, back-
dated commits, forking an existing
app as “yours,” plagiarism.
External evidence links (Google
Drive, Loom, YouTube) —
everything is committed in the
repo.
Committing real secrets / API keys
(redact to ${ENV}).
A private or missing repo at judging
time.
Everything you need is on this page — rules, stack, workflow, repo layout, evidence,
the full rubric, and the FAQ below. Your code, architecture (ARCHITECTURE.md),
screenshots, Slack export and configs all live in your repo. If something is genuinely
unclear, make a sensible call and note it in your README — that judgement is part of
the test. The only thing you ever need a human for is the one-time EastRouter $50
top-up (share your sign-up handle with a Forge captain).
10 · FAQ
Every doubt, pre-answered.
Do I have to finish every feature?
No. Land the 
 MUST  tier solidly, then go as deep as your sprints allow. We score how far
you got and how cleanly you ran the agent process. A finished Must-tier with a great loop
beats a half-broken Stretch attempt.
Do I have to deploy it live?
A live URL is a bonus, not a hard gate. What's required is that your app actually runs —
either we can run it from a fresh clone using your README steps (composer install,
migrate /-seed, npm run dev), or you give a working live URL. If you deploy, point your
frontend at the deployed API (not localhost).
Where do my screenshots, Slack export, and video go?
All inside your Git repo — nothing external. Configs in agents/, the loop in agent-log.md,
sprint docs in sprints/, Slack proof in slack-export/ (export) or slack-
export/screenshots/, and app/agent/CI screenshots in evidence/screenshots/ (see §06–
§07 for exact paths). There is no video this year. We do not accept Google Drive, Loom,
YouTube or any external link — if it isn't committed to the repo, it isn't counted. The
submission form only asks for your repo URL.
Why no video / no Drive this time?
It keeps judging fair, fast and permanent (links rot and sign-in walls cost people points last
time), and it's an in-person event — a judge will just ask you to run it live, which beats any
recording.
How exactly do I get my $50 EastRouter credit?
Sign up at dashboard.eastrouter.com with Google or GitHub, then share the email/handle
you signed up with to your Forge captain. They top you up to $50. A $10 trial works instantly
so you're never blocked while that happens. (See §02.)
Which model should I use?
z-ai/glm-5.1 is the recommended default for both planning and coding. For heavy
architecture/planning, give Hermes deepseek/deepseek-v4-pro (1M context, strong
reasoning). For cheap routine edits, drop to z-ai/glm-4.5-air or deepseek/deepseek-v4-
flash. Full list in §02.
Can I use Claude Code / Codex / Cursor / Cline as the coder instead of OpenClaw?
The event is specifically about the Hermes (orchestrator) + OpenClaw (coder) pairing over
Slack — that's what we grade and what your $50 is for. Use other tools only as a personal
editor; the graded loop must be Hermes →  OpenClaw. (EastRouter does support Claude
Code / Codex if you want them as helpers.)
What counts as a real “agent loop”?
A genuine exchange in Slack where you give Hermes a goal, Hermes plans & assigns a
scoped task to OpenClaw, OpenClaw implements + reports back, and you review/merge.
Real messages, real PRs, real commits — not a single screenshot of “bot joined the
channel,” and not a hand-written log pasted in after the fact.
An agent broke / hit a rate limit / produced garbage. What do I do?
That's normal — handling it is the skill. Re-scope the task smaller, switch to a different
EastRouter model, fix the config, and note what happened in agent-log.md. Honest
recovery from failure scores better than pretending it went perfectly.
Can I use a UI kit / component library / Laravel starter kit?
Yes — scaffolds, Tailwind UI, shadcn-style kits, Breeze/Jetstream for auth, etc. are all fine.
The PulseDesk features must be built by your agents during the event; boilerplate doesn't
have to be.
Do I need MySQL specifically? I used SQLite in the qualifier.
Yes, MySQL 8 for the final build (it's the spec, and it's a more realistic multi-tenant DB). Use
migrations + seeders so it sets up cleanly.
Should my repo be public or private?
Public. A private/missing repo can't be cloned and is a hard gate. Make it public before you
submit and keep it public until results are out.
What if I'm not done at 6:00 PM?
The form doesn't auto-close, so you can still submit — but the clock is real and late
submissions are reviewed at the organizers' discretion. Better to land a clean, smaller build
and submit on time than to chase a big one past the buzzer.
How and when do I find out if I won / how I did?
Same as the qualifier: a results page where you enter your GitHub username and see your
status + full, transparent score breakdown and feedback. Watch the Forge Slack / your
email.
Can I resubmit?
Yes — submit again and we score your latest entry. Use the same GitHub username each
time so we can match it.
Something here is ambiguous / I have a doubt not covered.
Make a reasonable decision, write it in your README, and keep moving — that judgement is
part of the test. For anything blocking, ask a Forge captain in person or in the Forge Slack.
11 · SUBMIT YOUR BUILD
Ship it. Submit here.
Make your repo public, confirm a fresh clone runs (or your live URL opens), tick the
SUBMISSION.md checklist, then submit. You can resubmit while the window is open — we
score your latest entry.
Before you submit (all committed in the repo): repo public · README has exact run
steps · migrate /-seed works · agents/hermes/ + agents/openclaw/ configs present ·
agent-log.md + sprints/ present · Slack proof in slack-export/ · screenshots in
evidence/screenshots/ · CI green. No external links.
Full name *
e.g. Priya Sharma
Email *
you@college.edu
WhatsApp / phone
+91 9XXXXXXXXX
College / institution
e.g. DTU, NSUT
Public GitHub repo URL *
https://github.com/you/forge2-you
Live URL (if deployed)
https://your-pulsedesk.vercel.app
EastRouter models used *
GLM 5.1 · DeepSeek V4 · ...
# of sprints you ran
e.g. 3
Anything we should know to run / judge it
Run steps, what works, what slipped, model routing, honest notes…
Just the repo URL — that's it. All your evidence (configs, agent-log.md, sprints, Slack
export/screenshots, app + CI screenshots) must already be committed inside that
repo at the paths in §06–§07. We don't take Google Drive, video, or any external link.
I confirm this is my own work built during the event, my repo is public, all my evidence is committed inside
the repo, and all model calls went through EastRouter.
Submit my Forge 2 build →
Forge 2 · Edition 1 — Main Hackathon · nmg.labs · private handbook, please don't share
publicly.
